### ✅ ARQUITETURA LIMPA

```
┌─────────────────┐
│  HTTP Request   │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│   Controller    │ (extends Controller)
└────────┬────────┘
         │ $this->service->create($dto)
         ▼
┌─────────────────┐
│     Service     │ (regras de negócio)
└────────┬────────┘
         │ $this->repository->create($dto)
         ▼
┌─────────────────┐
│    Repository   │ (acesso ao bando de dados)
└────────┬────────┘
         │ $this->model->where()
         ▼
┌─────────────────┐
│    Database     │
└─────────────────┘

🟢 Benefícios:
- 4 camadas (simples)
- Código explícito
- Desacoplado
- Fácil de testar
- Fácil de debugar
- Padrão Laravel standard
```